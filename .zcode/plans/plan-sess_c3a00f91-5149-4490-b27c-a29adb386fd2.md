## 阶段一：安全止血（Critical / High 问题修复）

**目标**：消除所有 Critical 和 High 级安全漏洞与稳定性问题。

### 主要任务
1. **修复 shell 命令注入**：对 DSL 传入的命令进行白名单校验 + 参数化执行。
2. **修复 Windows 沙箱空实现**：要么实现真实沙箱，要么在文档中声明 Windows 禁用插件。
3. **补全 gRPC 鉴权拦截器**：强制所有未显式跳过的方法验证 token，修复 tenant 为空时默认放行。
4. **修复审计 WORM 写入错误处理**：`Append` 方法在写入失败时返回 error。
5. **修复 AES-GCM nonce 处理**：确保 `rand.Read` 返回值检查。
6. **修复 SSH 连接 nil 解引用**：在 `defer conn.Close()` 前检查 conn。
7. **修复 batch 控制器 panic 保护**：增加 recover 确保 wg.Done 执行。
8. **为 HTTP 客户端增加超时**：禁止使用 `http.DefaultClient`，创建带超时的实例。

每个任务完成后运行相关单元测试验证，最后整体回归测试，确保无回归。

**验收标准**：所有 Critical 问题修复并通过新增单元测试；安全扫描（gosec）零 Critical/High 问题；基础功能在 Linux 和 Windows 上可正常运行（插件除外）。

我现在开始执行。